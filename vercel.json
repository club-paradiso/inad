{
  "framework": null,
  "installCommand": "echo 'INAD: static single-file release — no dependency install on Vercel'",
  "buildCommand": "mkdir -p dist && cp index.html dist/index.html",
  "outputDirectory": "dist",
  "cleanUrls": true,
  "trailingSlash": false
}
